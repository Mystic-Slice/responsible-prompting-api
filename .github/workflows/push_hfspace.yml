name: Sync to Hugging Face hub
on:
  push:
    branches: [hfspace]
  # to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true
      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: git push https://ashwath-vaithina-ibm:$HF_TOKEN@huggingface.co/spaces/ashwath-vaithina-ibm/resapi-github-fed main